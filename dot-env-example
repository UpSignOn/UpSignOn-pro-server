# DATABASE CONFIGURATION
DB_USER=upsignonpro
DB_PASS=db-password
DB_NAME=upsignonpro
DB_HOST=localhost
DB_PORT=5432

# THIS SERVER WILL NEED TO SEND EMAILS TO YOUR USERS
EMAIL_HOST=smtp.votre-domaine.fr
EMAIL_PORT=587
EMAIL_USER=no-reply@votre-domaine.fr
EMAIL_PASS=password # can be empty

# UPSIGNON PRO SERVER HOST
#  - without https://
#  - with path if needed
API_PUBLIC_HOSTNAME=upsignonpro.votre-domaine.fr

# OPTIONAL LOCAL SSL CONFIGURATION
# Not necessary if you use a reverse-proxy (Nginx), as described in this documentation
# Defining these paths will make the nodejs server use HTTPS
#  - Use absolute paths
#  - File extensions don't matter, .key and .cer or .pem will do
#  - Do not forget to chmod 400 these files
SSL_CERTIFICATE_KEY_PATH=
SSL_CERTIFICATE_CRT_PATH=

# DO NOT CHANGE
NODE_ENV=production
SERVER_PORT=3000
